#Simple IF statement that checks for a condition. If anything is written to console then the installation is deemed to be a success, so important the ELSE statement block must be empty.

If (condition) { 
    Write-Host "Installed" 
} else { 
}